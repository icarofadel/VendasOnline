server:
  port: 8082
  wait-time-in-ms-when-sync-empty: 3000

spring:
  spring:
    profiles:
      active: native
  data:
      mongodb:
        uri: mongodb://127.0.0.1:27017/VendasOnline
        database: Produto
        auto-index-creation: true

application-version: 1.0